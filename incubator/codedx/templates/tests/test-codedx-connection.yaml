apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Release.Name }}-connection-test"
  labels:
    {{- include "codedx.commonLabels" . | nindent 4}}
    codedx.com-instance: {{ .Release.Name | quote }}
    codedx.com-component: tests
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
  - name: {{ .Release.Name }}