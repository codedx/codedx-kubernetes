{{- if .Values.podSecurityPolicy.create -}}
apiVersion: policy/v1beta1
kind: PodSecurityPolicy
metadata:
  name: {{ include "codedx-tool-orchestration.rbac.psp.name" . | quote }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "codedx-tool-orchestration.commonLabels" . | nindent 4 }}
  annotations:
{{- toYaml .Values.podSecurityPolicy.annotations | trim | nindent 4 }}
spec:
  privileged: false
  allowPrivilegeEscalation: false
  volumes: ["secret", "emptyDir"]
  hostNetwork: false
  hostIPC: false
  hostPID: false
  supplementalGroups:
    rule: MustRunAs
    ranges:
    - min: 1
      max: 65535
  fsGroup:
    rule: MustRunAs
    ranges:
    - min: 1
      max: 65535
  runAsUser:
    rule: MustRunAsNonRoot
  seLinux:
    rule: RunAsAny
{{- end -}}